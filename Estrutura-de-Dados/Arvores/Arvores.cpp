// Arvores.cpp : Defines the entry point for the console application.
//
//Arvore binaria de busca

#include "stdafx.h"
#include <iostream>
#include <fstream>
#include <string>

#include <algorithm>
#include "Arvore.h"
#include "Caminhamentos.h"
using namespace std;
//
//void gravar(string th, string op, int ocup, int instr) {
//	ofstream arq("saida.csv",ofstream::app);
//	arq << th << "," 
//		<< op << "," 
//		<< ocup << "," 
//		<< instr << "\n";
//}

int main()
{
	//THEA h;

	//inicializar(h);
	//srand(time(NULL));
	//int instr;
	//for (int i = 1; i <= 2000; i++) {
	//	int chave = rand() % 32000 + 1;
	//	instr = 1;
	//	inserirTH(h, chave, instr);
	//	if (i == 90 || i == 240 || i == 740 || i == 990 || i == 1990) {
	//		for (int k = 0; k < 10; k++) {
	//			int c = rand() % 32000 + 1;
	//			instr = 1;
	//			inserirTH(h, c, instr);
	//			gravar("EA", "ins", (i + 10) / 10, instr);
	//		}

	//		for (int k = 0; k < 10; k++) {
	//			int c = rand() % 32000 + 1;
	//			instr = 1;
	//			pesquisa(h, c, instr);
	//			gravar("EA", "pesq", (i + 10) / 10, instr);
	//		}
	//		i += 10;
	//	}
	//}
    return 0;
}

