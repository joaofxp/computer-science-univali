#include "stdafx.h"
#include <iostream>
#include <string>

//Realize as implementações abaixo, para que as funções sejam capazes de retornar se a operação é válida e também oriente de alguma forma o usuário quanto aos erros. Monte uma biblioteca com estas funções de validação.

using namespace std;

//bool validarNumeroTelefoneCelular(string telefone); //(47)XXXXX-XXX
//bool validarNumeroCPF(string cpf); //XXX.XXX.XXX-XX
//bool dividirDoisNumeros(int numero1, int numero2); //que erros podem ocorrer?
//bool escolherOpcaoMenu(int opcao); //o que dizer se o usuário digitar uma opção inválida?
//bool validarSenha(string campoSenha, string campoConfirmaSenha);//Tem mesmo tamanho? são iguais? estão preenchidas? Identifique os erros
//bool validarEmail(string email); //Para considerar um válido e-mail, trate que deve ter um @ e um .

int main()
{
	return 0;
}

